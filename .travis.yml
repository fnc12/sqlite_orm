
language: cpp

sudo: required

compiler:
  - clang

os:
  - linux

install:
  - sudo apt-get install sqlite3 libsqlite3-dev

script:
  - clang++ -std=c++14 examples/in_memory.cpp -I include/sqlite_orm/ -lsqlite3